# Server
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# Simulation
SIMULATION_TICK_MS=500
MAX_SERVERS=12
DEFAULT_ALGORITHM=round_robin
DEFAULT_TRAFFIC_MODE=normal

# Default server settings
DEFAULT_SERVER_MAX_CONNECTIONS=100
DEFAULT_SERVER_WEIGHT=1

# SMTP Configuration (For Feedback)
# REQUIRED: Replace 'YOUR_APP_PASSWORD_HERE' with a 16-character Gmail App Password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=arorapratham758@gmail.com
SMTP_PASSWORD=dydfytnnukolosyv
SMTP_TLS=True
