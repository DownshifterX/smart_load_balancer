services:
  - type: web
    name: nexus-load-balancer
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: LOG_LEVEL
        value: INFO
      - key: RESEND_API_KEY
        sync: false
